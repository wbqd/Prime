'use strict';

/**
 * @ngdoc function
 * @name primeApp.controller:ScheduleCtrl
 * @description
 * # ScheduleCtrl
 * Controller of the primeApp
 */
angular.module('primeApp')
  .controller('ScheduleCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
